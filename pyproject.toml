[project]
name = "app"
version = "1.0"

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]

[tool.isort]
profile = "black"

[tool.black]
line-length = 106